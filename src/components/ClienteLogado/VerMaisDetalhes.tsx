import Link from 'next/link';

export default function VerMaisDetalhes(props: any) {
  return (
      <div style={{ cursor: 'pointer', textDecoration: 'underline' }}>Clique aqui para saber mais sobre o evento</div>
  );
}
